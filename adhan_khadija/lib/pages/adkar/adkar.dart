import 'package:flutter/material.dart';

class Adkar extends StatelessWidget {
  const Adkar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('صفحة الأذكار')));
  }
}
