name: adhan_khadija
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  animated_analog_clock: ^0.2.0  # Keep this version
  arabic_font: ^0.0.9
  salomon_bottom_bar_extend: ^3.3.2
  adhan: ^2.0.0
  just_audio: ^0.10.5
  audio_session: ^0.2.2
  audio_service: ^0.18.15
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  permission_handler: ^12.0.1
  flutter_local_notifications: ^19.5.0
  timezone: ^0.10.0
  flutter_timezone: ^5.0.1
  intl: ^0.20.2
  provider: ^6.0.5
  lottie: ^3.3.2
  connectivity_plus: ^6.1.0
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/mosque.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/mosque.png"

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/mosque.png
    - assets/audio/
    - assets/lottie/